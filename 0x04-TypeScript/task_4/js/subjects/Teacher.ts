// Namespace to group related types and classes
namespace Subjects {
  // Interface representing a Teacher
  export interface Teacher {
    // Property to hold the teacher's first name
    firstName: string;
    // Property to hold the teacher's last name
    lastName: string;
  }
}
